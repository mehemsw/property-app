# from django.urls import path
# from .views import PopulationRequest


# urlpatterns = [
#     # utilize the registered converter to ensure the parameter is valid
#     path("population/<int:id>/", PopulationRequest.as_view(), name='population_request'),
# ]